import 'package:flutter/material.dart';

class msg_reply extends StatelessWidget {
  const msg_reply({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
       ],
    );
  }
}