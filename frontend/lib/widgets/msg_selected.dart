import 'package:flutter/material.dart';

class msg_selected extends StatelessWidget {
  const msg_selected({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
       ],
    );
  }
}